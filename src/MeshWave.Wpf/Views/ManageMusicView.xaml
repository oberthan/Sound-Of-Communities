<UserControl x:Class="MeshWave.Wpf.Views.ManageMusicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
            <TextBlock Text="My Uploads" FontSize="32" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Upload New Track" Command="{Binding AddTrackCommand}"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    Margin="30,0,0,0" Padding="20,10" FontWeight="Bold"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding MyTracks}"
                  Background="Transparent" BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" Width="200"/>
                    <GridViewColumn Header="Album" DisplayMemberBinding="{Binding AlbumId}" Width="150"/>
                    <GridViewColumn Header="File Path" DisplayMemberBinding="{Binding FilePath}" Width="300"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
