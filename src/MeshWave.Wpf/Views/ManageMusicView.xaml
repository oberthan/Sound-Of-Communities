<UserControl x:Class="MeshWave.Wpf.Views.ManageMusicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,30">
            <StackPanel>
                <TextBlock Text="My Library" Style="{StaticResource Header1}"/>
                <TextBlock Text="Manage your uploads and local collections" Foreground="{StaticResource TextDimBrush}" Margin="0,-15,0,20"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button Content="Add Files" Command="{Binding AddFileCommand}" Padding="20,10" Margin="0,0,10,0" Background="{StaticResource SurfaceBrush}" BorderThickness="1" BorderBrush="{StaticResource SurfaceLightBrush}"/>
                <Button Content="Add Folder" Command="{Binding AddFolderCommand}" Padding="20,10" Background="{StaticResource PrimaryBrush}" Foreground="Black" FontWeight="Bold"/>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="1" ItemsSource="{Binding MyTracks}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" Width="300" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="Status" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="Synchronized" Foreground="{StaticResource PrimaryBrush}" FontSize="11"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Path" Width="400" DisplayMemberBinding="{Binding FilePath}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
