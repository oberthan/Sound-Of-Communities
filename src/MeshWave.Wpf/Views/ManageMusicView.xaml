<UserControl x:Class="MeshWave.Wpf.Views.ManageMusicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
            <TextBlock Text="My Uploads" FontSize="32" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Add Files" Command="{Binding AddFileCommand}"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    Margin="30,0,0,0" Padding="20,10" FontWeight="Bold"/>
            <Button Content="Add Folder" Command="{Binding AddFolderCommand}"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    Margin="10,0,0,0" Padding="20,10" FontWeight="Bold"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding MyTracks}"
                  Background="Transparent" BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" Width="200"/>
                    <GridViewColumn Header="Album" DisplayMemberBinding="{Binding AlbumId}" Width="150"/>
                    <GridViewColumn Header="Duration" DisplayMemberBinding="{Binding Duration, StringFormat=\{0:mm\\:ss\}}" Width="100"/>
                    <GridViewColumn Header="Actions">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Play" Command="{Binding DataContext.PlayCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
