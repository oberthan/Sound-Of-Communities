<UserControl x:Class="MeshWave.Wpf.Views.ManageMusicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="My Music" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Add Track" Command="{Binding AddTrackCommand}" Margin="20,0,0,0" Padding="10,5"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding MyTracks}" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" Width="200"/>
                    <GridViewColumn Header="Album" DisplayMemberBinding="{Binding AlbumId}" Width="150"/>
                    <GridViewColumn Header="File Path" DisplayMemberBinding="{Binding FilePath}" Width="300"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
