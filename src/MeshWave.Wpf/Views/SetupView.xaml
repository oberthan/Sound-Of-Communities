<UserControl x:Class="MeshWave.Wpf.Views.SetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="40">
        <StackPanel MaxWidth="600" HorizontalAlignment="Left">
            <TextBlock Text="Initial Configuration" FontSize="32" FontWeight="Bold" Margin="0,0,0,30"/>

            <TextBlock Text="Artist Name" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,8"/>
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                     Padding="12" Background="#2A2A2A" Foreground="White" BorderThickness="0" Margin="0,0,0,25"/>

            <TextBlock Text="Local Storage Path" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,8"/>
            <Grid Margin="0,0,0,40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding StoragePath}" IsReadOnly="True"
                         Padding="12" Background="#2A2A2A" Foreground="White" BorderThickness="0" VerticalAlignment="Center"/>
                <Button Content="Change Folder" Grid.Column="1" Margin="10,0,0,0"
                        Command="{Binding BrowseCommand}" Padding="15,10"/>
            </Grid>

            <Button Content="Finish Setup" Command="{Binding SaveCommand}"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    Padding="30,15" HorizontalAlignment="Left" FontWeight="Bold" FontSize="16"/>

            <TextBlock Text="Your library metadata and synchronized music will be stored here."
                       FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,15,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
