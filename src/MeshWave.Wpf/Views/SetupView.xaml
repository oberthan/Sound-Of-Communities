<UserControl x:Class="MeshWave.Wpf.Views.SetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="MeshWave Setup" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="User Name:"/>
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>

            <TextBlock Text="Storage Folder:"/>
            <Grid Margin="0,5,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding StoragePath}" IsReadOnly="True" VerticalAlignment="Center"/>
                <Button Content="Browse..." Grid.Column="1" Margin="5,0,0,0" Command="{Binding BrowseCommand}"/>
            </Grid>

            <Button Content="Save and Continue" Command="{Binding SaveCommand}" Padding="20,10" HorizontalAlignment="Left" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
