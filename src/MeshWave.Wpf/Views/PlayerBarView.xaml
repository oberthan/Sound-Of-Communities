<UserControl x:Class="MeshWave.Wpf.Views.PlayerBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="90" d:DesignWidth="1200">
    <Grid Margin="15,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Track Info -->
        <StackPanel Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal">
            <Border Width="56" Height="56" Background="{StaticResource SurfaceLightBrush}" CornerRadius="4">
                <TextBlock Text="♪" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource TextDimBrush}"/>
            </Border>
            <StackPanel Margin="15,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding CurrentTrack.Title, FallbackValue='Not Playing'}" FontWeight="Bold" FontSize="14" MaxWidth="200" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding CurrentTrack.ArtistName, FallbackValue='Unknown Artist'}" FontSize="12" Foreground="{StaticResource TextDimBrush}"/>
            </StackPanel>
        </StackPanel>

        <!-- Controls -->
        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                <Button Content="⏮" Style="{StaticResource SidebarButton}" FontSize="18"/>
                <Button Content="{Binding PlayPauseIcon}" Command="{Binding PlayPauseCommand}"
                        Background="White" Foreground="Black" Width="36" Height="36" Margin="15,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="18">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="⏭" Style="{StaticResource SidebarButton}" FontSize="18"/>
            </StackPanel>

            <Grid Width="500">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding CurrentTimeStr}" FontSize="11" Foreground="{StaticResource TextDimBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>

                <Grid Grid.Column="1">
                    <ItemsControl ItemsSource="{Binding WaveformPoints}" VerticalAlignment="Center" Height="20">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="3" Margin="1,0" Background="{StaticResource PrimaryBrush}"
                                        Opacity="0.3" VerticalAlignment="Center"
                                        Height="{Binding Path=., Converter={StaticResource HeightScaleConverter}, ConverterParameter=20}">
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Slider Maximum="100" Value="{Binding Progress}" VerticalAlignment="Center"
                            Style="{StaticResource {x:Type Slider}}" Opacity="0.5"/>
                </Grid>

                <TextBlock Grid.Column="2" Text="{Binding DurationStr}" FontSize="11" Foreground="{StaticResource TextDimBrush}" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </Grid>
        </StackPanel>

        <!-- Volume & Extras -->
        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
             <Button Content="💬" Style="{StaticResource SidebarButton}" Margin="0,0,10,0"/>
            <TextBlock Text="🔊" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <Slider Width="80" Maximum="1" Value="{Binding Volume}" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
