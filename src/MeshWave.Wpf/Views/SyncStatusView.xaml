<UserControl x:Class="MeshWave.Wpf.Views.SyncStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Mesh Network Status" FontSize="32" FontWeight="Bold" Margin="0,0,0,30"/>

            <Border Background="#2A2A2A" Padding="20" CornerRadius="8" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Ellipse Width="12" Height="12" Fill="{StaticResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Connected to Mesh Network" FontWeight="Bold" FontSize="16"/>
                        <TextBlock Text="8 active peers discovered" Foreground="{StaticResource SecondaryTextBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <TextBlock Text="Sync Activity" FontSize="18" FontWeight="Bold" Margin="0,10,0,15"/>
            <ItemsControl>
                <Border Background="#1A1A1A" Padding="15" Margin="0,0,0,10" CornerRadius="4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Downloading 'Nebula' by StarGazer..."/>
                        <TextBlock Grid.Column="1" Text="45%" Foreground="{StaticResource AccentBrush}"/>
                    </Grid>
                </Border>
                <Border Background="#1A1A1A" Padding="15" Margin="0,0,0,10" CornerRadius="4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Broadcasting your new track 'Morning Dew'"/>
                        <TextBlock Grid.Column="1" Text="Complete" Foreground="{StaticResource AccentBrush}"/>
                    </Grid>
                </Border>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
