<UserControl x:Class="MeshWave.Wpf.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding CurrentTrack.Title, FallbackValue='No Track Selected'}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Waveform Visualization Placeholder -->
        <Border Grid.Row="1" Background="#1A1A1A" CornerRadius="8">
            <Grid>
                <Canvas Name="WaveformCanvas">
                    <Polyline Points="0,50 10,40 20,60 30,30 40,70 50,50 60,80 70,20 80,60 90,40 100,70 110,30 120,60 130,40 140,80 150,20 160,50 170,70 180,30 190,60 200,40 210,80 220,50 230,20 240,60 250,40 260,70 270,30 280,60 290,40 300,80 310,20 320,50 330,70 340,30 350,60 360,40 370,80 380,50 390,20 400,60 410,40 420,70 430,30 440,60 450,40 460,80 470,20 480,50 490,70 500,30 510,60 520,40 530,80 540,50 550,20 560,60 570,40 580,70 590,30 600,60 610,40 620,80 630,20 640,50 650,70 660,30 670,60 680,40 690,80 700,50 710,20 720,60 730,40 740,70 750,30 760,60 770,40 780,80 790,20 800,50"
                              Stroke="{StaticResource AccentBrush}" StrokeThickness="1" Opacity="0.6"/>

                    <!-- Playback Cursor -->
                    <Rectangle Width="2" Fill="#FFFFFF" Height="100">
                        <Rectangle.RenderTransform>
                            <TranslateTransform X="{Binding Progress, Converter={StaticResource ProgressToXConverter}, ConverterParameter=800}"/>
                        </Rectangle.RenderTransform>
                    </Rectangle>
                </Canvas>

                <!-- Interaction layer -->
                <Rectangle Fill="Transparent" Cursor="Hand" MouseLeftButtonDown="Waveform_MouseDown"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="Play/Pause" Command="{Binding PlayPauseCommand}" Padding="10,5"/>
            <TextBlock Text="{Binding CurrentTime, StringFormat=\{0:mm\\:ss\}}" Margin="20,0" VerticalAlignment="Center"/>
            <Button Content="Add Comment" Command="{Binding AddCommentCommand}" Padding="10,5"/>
        </StackPanel>

        <ListView Grid.Row="3" ItemsSource="{Binding Comments}"
                  Background="Transparent" BorderThickness="0" Margin="0,20,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="40" Height="40" CornerRadius="20" Background="#333" Margin="0,0,15,0">
                            <TextBlock Text="?" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="User" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                                <TextBlock Text="{Binding TrackTimestamp, StringFormat=' at \{0:mm\\:ss\}'}" Foreground="{StaticResource SecondaryTextBrush}" Margin="5,0,0,0"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
