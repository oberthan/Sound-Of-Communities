<UserControl x:Class="MeshWave.Wpf.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Main Display: Cover & Info -->
        <StackPanel VerticalAlignment="Center">
            <Border Width="300" Height="300" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Margin="0,0,0,30" Effect="{DynamicResource {x:Static SystemParameters.DropShadowKey}}">
                 <TextBlock Text="♪" FontSize="120" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource SurfaceLightBrush}"/>
            </Border>
            <TextBlock Text="{Binding CurrentTrack.Title, FallbackValue='Not Playing'}" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding CurrentTrack.ArtistName, FallbackValue='Unknown Artist'}" FontSize="24" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Comments UI -->
        <Grid Grid.Row="2" Margin="0,40,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Comments" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

            <ListView Grid.Row="1" ItemsSource="{Binding Comments}" Background="Transparent" BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="User" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                                <TextBlock Text="{Binding TrackTimestamp, StringFormat=' at \{0:mm\\:ss\}'}" Foreground="{StaticResource TextDimBrush}" Margin="5,0,10,0"/>
                                <TextBlock Text="{Binding Content}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding NewCommentText, UpdateSourceTrigger=PropertyChanged}"
                         Padding="10" Background="{StaticResource SurfaceBrush}" Foreground="White" BorderThickness="0"/>
                <Button Grid.Column="1" Content="Post" Command="{Binding PostCommentCommand}"
                        Padding="20,10" Margin="10,0,0,0" Background="{StaticResource PrimaryBrush}" Foreground="Black" FontWeight="Bold"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
