<UserControl x:Class="MeshWave.Wpf.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding CurrentTrack.Title, FallbackValue='No Track Selected'}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Waveform Visualization Placeholder -->
        <Border Grid.Row="1" Background="#f0f0f0" BorderBrush="DarkGray" BorderThickness="1">
            <Canvas Name="WaveformCanvas">
                <Polyline Points="0,50 50,20 100,80 150,40 200,60 250,30 300,70 350,50 400,20 450,80 500,40 550,60 600,30 650,70 700,50 750,20 800,80"
                          Stroke="Blue" StrokeThickness="2" VerticalAlignment="Center"/>

                <!-- Playback Cursor -->
                <Rectangle Width="2" Fill="Red" Height="100">
                    <Rectangle.RenderTransform>
                        <TranslateTransform X="{Binding Progress, Converter={StaticResource ProgressToXConverter}, ConverterParameter=800}"/>
                    </Rectangle.RenderTransform>
                </Rectangle>
            </Canvas>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="Play/Pause" Command="{Binding PlayPauseCommand}" Padding="10,5"/>
            <TextBlock Text="{Binding CurrentTime, StringFormat=\{0:mm\\:ss\}}" Margin="20,0" VerticalAlignment="Center"/>
            <Button Content="Add Comment" Command="{Binding AddCommentCommand}" Padding="10,5"/>
        </StackPanel>

        <ListView Grid.Row="3" ItemsSource="{Binding Comments}" Margin="0,10,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time" DisplayMemberBinding="{Binding TrackTimestamp, StringFormat=\{0:mm\\:ss\}}" Width="60"/>
                    <GridViewColumn Header="Comment" DisplayMemberBinding="{Binding Content}" Width="400"/>
                    <GridViewColumn Header="User" DisplayMemberBinding="{Binding UserId}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
