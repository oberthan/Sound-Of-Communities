<Window x:Class="MeshWave.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MeshWave.Wpf.ViewModels"
        xmlns:views="clr-namespace:MeshWave.Wpf.Views"
        mc:Ignorable="d"
        Title="MeshWave" Height="600" Width="900">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:SetupViewModel}">
            <views:SetupView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LibraryViewModel}">
            <views:LibraryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ManageMusicViewModel}">
            <views:ManageMusicView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PlayerViewModel}">
            <views:PlayerView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="#e0e0e0">
            <TextBlock Text="MeshWave" FontSize="24" FontWeight="Bold" Margin="10,20"/>
            <Button Content="Library" Command="{Binding NavigateLibraryCommand}"/>
            <Button Content="My Music" Command="{Binding NavigateManageCommand}"/>
            <Button Content="Player" Command="{Binding NavigatePlayerCommand}"/>
            <Button Content="Setup" Command="{Binding NavigateSetupCommand}" VerticalAlignment="Bottom" Margin="5,50,5,5"/>
        </StackPanel>

        <ContentControl Grid.Column="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>
