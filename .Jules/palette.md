# Palette's UX Journal

## 2025-05-15 - [Interactive Icons in Lists]
**Learning:** Using `TextBlock` with mouse events for interactive elements in lists prevents keyboard navigation and screen reader discovery.
**Action:** Use `Button` elements with appropriate templates, `AutomationProperties.Name`, and `ToolTip` for all interactive list actions.
